//! Selection screens for context and service.
//!
//! These are lightweight screens (not full services) used during the app's
//! initial selection phases before a service is active.

pub mod context_selector;
pub mod service_selector;
